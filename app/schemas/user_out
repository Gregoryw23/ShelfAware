from datetime import datetime
from pydantic import BaseModel, EmailStr

class UserOut(BaseModel):
    user_id: str
    email: EmailStr
    status: str
    created_at: datetime
    
    # allows FastAPI to read SQLAlchemy models
    class Config:
        orm_mode = True  